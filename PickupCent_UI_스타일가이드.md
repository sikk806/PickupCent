# PickupCent — UI 스타일 가이드

> 이전에 만든 웹 프로토타입의 UI 룩앤필을 Unity에서 재현하기 위한 디자인 스펙입니다.
> 코드를 그대로 옮기는 게 아니라, 색상/치수/구조를 수치로 정리한 것이니 Unity UI(uGUI 또는 UI Toolkit)로 새로 구현하면 됩니다.

---

## 1. 색상 팔레트

| 이름 | 값 | 용도 |
|---|---|---|
| Wood Dark | `#4A3323` | 어두운 나무색, 버튼 텍스트/보더 |
| Wood Light | `#8A6440` | 밝은 나무색, 보더 하이라이트 |
| Cream | `#F6ECD9` | 밝은 본문 텍스트 |
| Gold | `#E0A92E` | 버튼 그라디언트 하단, 강조 보더 |
| Gold Bright | `#FFD166` | 버튼 그라디언트 상단, 숫자/포인트 강조색 |
| Ink | `#2E2115` | 가장 어두운 텍스트 |
| Panel BG | `#241A11` | 패널 기본 배경 |

패널/HUD 배경은 대부분 위 색상에 **반투명도**를 섞어서 씁니다.
- 사이드패널 블록 배경: `rgba(36,26,17,0.88)` (거의 불투명한 짙은 갈색)
- HUD 알약(pill) 배경: `rgba(46,33,21,0.78)`
- 테두리: 흰색 반투명 `rgba(255,255,255,0.14~0.18)` 정도의 얇은 보더

## 2. 타이포그래피

- **제목/버튼/숫자류**: 둥글고 통통한 느낌의 디스플레이 폰트(웹에서는 Google Fonts의 "Baloo 2" 사용). Unity에서는 이와 비슷한 느낌의 라운드체 한글/영문 폰트를 구해서 TextMeshPro Font Asset으로 만들어 쓰는 걸 추천합니다. Bold(700~800 굵기) 위주로 사용합니다.
- **본문/설명 텍스트**: 깔끔한 산세리프(웹에서는 "Nunito"). 이것도 비슷한 느낌의 폰트로 대체하면 됩니다.
- 숫자(타이머, 점수)는 자릿수가 바뀌어도 폭이 흔들리지 않는 고정폭 숫자 스타일을 씁니다.

## 3. 전체 레이아웃 구조

- 화면은 크게 두 영역으로 나뉩니다: **왼쪽(또는 중앙)의 게임 맵 영역**과, **맵과 완전히 분리된 오른쪽 사이드패널**입니다. 맵 위에 버튼이나 리스트가 겹쳐서 안 보이게 하는 게 핵심 원칙입니다.
- 맵 위에는 최소한의 정보(지역명, 보유 금액, 타이머, 장착 도구 내구도)만 알약(pill) 모양 배지로 화면 상단 중앙에 떠 있습니다. 이건 게임 화면 위에 겹쳐도 괜찮은 정보로 취급합니다.
- 사이드패널은 세로로 여러 개의 "블록" 카드가 쌓인 구조입니다. 각 블록은 둥근 모서리에 반투명 어두운 배경, 얇은 흰색 테두리를 가진 카드입니다.
- 사이드패널 맨 아래에는 상점/지역 이동 같은 주요 버튼들이 세로로 나열됩니다.

## 4. 컴포넌트별 스타일

### 4-1. 기본 버튼 (.btn)
- 배경: 위에서 아래로 흐르는 그라디언트, 밝은 골드(`#FFD166`)에서 진한 골드(`#E0A92E`)로.
- 텍스트: Wood Dark 색, 굵게.
- 테두리: 위/좌우 없음, **아래쪽에만 두꺼운(4px) 진한 골드-브라운 보더**(`#A06D15`)가 있어서 마치 버튼이 살짝 튀어나온(눌리는) 느낌을 줍니다.
- 모서리: 둥글게(14px 정도).
- 그림자: 은은한 검은 그림자를 아래로 깔아서 입체감을 줍니다.
- 눌렀을 때(클릭 중): 버튼이 아래로 살짝(3px) 내려가고 아래쪽 보더가 얇아지면서 "눌린" 느낌을 냅니다. 이게 이 UI의 시그니처 상호작용입니다.
- 보조 버튼(덜 중요한 액션): 그라디언트 없이 반투명 흰색 배경, 크림색 텍스트로 톤을 낮춥니다.

### 4-2. 사이드패널 블록 (.sp-block)
- 배경: 짙은 갈색 반투명(`rgba(36,26,17,0.88)`).
- 테두리: 얇은 흰색 반투명(2px, `rgba(255,255,255,0.14)`).
- 모서리: 둥글게(14px).
- 안쪽 여백은 넉넉하게(12~14px 정도).
- 그림자: 아래로 은은하게 깔림.

### 4-3. 상단 HUD 알약(pill)
- 배경: 짙은 갈색 반투명(`rgba(46,33,21,0.78)`), 뒤에 살짝 블러 효과가 있으면 더 좋습니다(선택 사항).
- 모서리: 완전히 둥근 캡슐 모양(양 끝이 반원).
- 안에 아이콘/라벨 + 숫자가 가로로 나란히 배치됩니다. 숫자 부분만 Gold Bright 색으로 강조합니다.
- 테두리: 얇은 흰색 반투명.

### 4-4. 목록 항목(상점 아이템 줄, 강화 항목 줄 등)
- 각 항목은 가로로 긴 카드 형태입니다: 왼쪽에 아이콘, 가운데에 이름+설명, 오른쪽에 버튼/수치.
- 배경은 사이드패널 블록보다 살짝 더 옅은 반투명 흰색(`rgba(255,255,255,0.055)` 정도)으로, 블록 안에서도 한 단계 더 옅은 느낌을 줍니다.
- 테두리: 아주 얇은 흰색 반투명.
- 현재 장착/적용 중인 항목은 테두리 색을 옅은 하늘색 계열로 바꿔서 구분합니다.

### 4-5. 탭 버튼(카테고리 전환)
- 여러 개가 가로로 나란히, 폭을 균등하게 나눠 갖습니다.
- 비활성 탭: 옅은 반투명 배경, 흐린 텍스트.
- 활성 탭: 4-1의 기본 버튼과 같은 골드 그라디언트로 강조됩니다.

## 5. 전반적인 느낌 (한 줄 요약)

따뜻한 나무/모래 톤 배경 위에, 골드색 버튼이 살짝 눌리는 손맛이 있는 상호작용을 하고, 사이드패널은 짙은 갈색 반투명 카드들이 쌓인 구조. 전체적으로 아기자기하고 둥글둥글한 느낌(라운드 폰트, 둥근 모서리, 캡슐형 알약 배지)이 핵심입니다. 각지고 딱딱한 느낌보다는 부드럽고 손에 잡히는 느낌을 유지해 주세요.
